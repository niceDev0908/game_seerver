export interface IUserInfo {
    id: number;
    name: string;
    email: string;
    companyId?: string;
    companyName?: string;
}

export interface UserInfo{
    name: string;
    roomId: string;
}
